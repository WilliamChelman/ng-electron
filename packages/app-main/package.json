{
  "name": "app-main",
  "version": "0.0.1",
  "main": "out-tsc/main.js",
  "license": "MIT",
  "scripts": {
    "start": "npm run build && electron .",
    "build": "tsc -p ./tsconfig.json",
    "start:build": "cross-env PACKAGE=true npm-run-all build start"
  },
  "dependencies": {
    "electron": "1.7.9"
  },
  "devDependencies": {
    "@types/dotenv": "^4.0.2",
    "cross-env": "^5.1.0",
    "dotenv": "^4.0.0",
    "electron-reload": "^1.2.2",
    "npm-run-all": "^4.1.1"
  }
}
